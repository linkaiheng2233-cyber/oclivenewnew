# TTS (sherpa-onnx OfflineTts / Piper). Install into voice-loop-minimal venv:
#   pip install -r requirements-tts.txt
-r requirements-asr.txt
# Optional cloud profile edge-tts-zh (no GPU):
edge-tts>=7.0.0
