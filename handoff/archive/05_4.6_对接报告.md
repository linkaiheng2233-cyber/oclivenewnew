# oclivenewnew — 4.6 对接进度报告

**报告日期**：2026-04-01  
**用途**：**当前进度快照** + 代码锚点；契约与防呆清单以 **`00` / `01` / `04`** 及源码为准。

---

## 1. 进度快照（WEEK3 + WEEK3-003）

- **编排**：唯一入口 `domain/chat_engine::process_message`；Tauri 仅注册 `send_message`（`lib.rs` → `generate_handler!`）。
- **持久化**：成功路径含 **`save_memory` → `DbManager::save_event`**（写入 `events` 表）。
- **测试**：`src-tauri/tests/chat_integration.rs` — `MockLlmClient`、`AppState::new_in_memory_with_llm`；断言与 DB 一致，**不硬编码**特定 `EventType`（`EventDetector` 的 bot 侧当前为 **`Neutral`**，部分文案不会稳定触发如 Quarrel）。
- **待办**：更多 `api/*`、前端 `invoke` 薄封装、可选 **单事务**（记忆与事件）、bot 情绪从状态/库读取。

---

## 2. 代码锚点

| 主题 | 路径 |
|------|------|
| 编排与 `save_event` 顺序 | `src-tauri/src/domain/chat_engine/` |
| LLM 抽象 + `MockLlmClient` | `src-tauri/src/infrastructure/llm.rs` |
| 集成测试 | `src-tauri/tests/chat_integration.rs` |
| 内存 State + 注入 LLM | `src-tauri/src/state/mod.rs`（`new_in_memory_with_llm`） |
| DTO | `src-tauri/src/models/dto.rs` |
| Schema | `src-tauri/migrations/001_init.sql` |

**crate 标识符**：`oclivenewnew_tauri`（集成测试 `use oclivenewnew_tauri::...`）。

---

## 3. 环境与联调

- **无默认 REST**：`invoke('send_message', …)`；Ollama：`OLLAMA_BASE_URL`、`OLLAMA_MODEL`。
- **角色**：`./roles/<id>/`；集成测试依赖相对 `CARGO_MANIFEST_DIR` 的 `../roles`（如 `mumu`）。
- **DB**：生产 `./data/app.db`；测试用内存库。

---

## 4. 质量门禁（基线）

```bash
cd src-tauri
cargo fmt --check
cargo clippy --all-targets -- -D warnings
cargo test --tests
```

---

## 5. 已知限制（简）

| 项 | 说明 |
|----|------|
| 事务 | `save_memory` 与 `save_event` 非同一事务；失败时可能不一致。 |
| bot 情绪 | 传入 `EventDetector` 的为 **`Neutral`**。 |
| 前端 | 响应字段 **`reply`**，非 `response`。 |

---

## 6. 阅读顺序

`00_HANDOFF_SUMMARY.md` → `04_4.6_PROJECT_TRUTH_CHECKLIST.md` → `01_DEVELOPMENT_REPORT.md` → `02` / `03`（按需）。本文件作 **进度速查**。
