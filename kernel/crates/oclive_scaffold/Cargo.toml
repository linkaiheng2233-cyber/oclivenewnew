[package]
name = "oclive_scaffold"
version = "0.1.0"
edition = "2021"
description = "Versioned Scaffold Package contracts and local discovery for OCLive"
license = "Apache-2.0"

[lints]
workspace = true

[dependencies]
semver = { workspace = true }
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
sha2 = { workspace = true }
tempfile = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
