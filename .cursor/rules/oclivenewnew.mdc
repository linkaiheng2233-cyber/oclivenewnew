---
description: oclivenewnew Tauri+Rust 项目约束（编排、Repository、DTO、迁移）
globs: "**/*"
alwaysApply: true
---

# oclivenewnew

1. **编排**：对话主流程在 `kernel/crates/oclive_kernel_host/src/domain/chat_engine/process_message.rs` 的 `process_message`（经 `chat_engine/mod.rs` re-export）；业务公式留在各 `*_engine` / analyzer，避免 API 层堆逻辑。Tauri 命令在 `distros/desktop-tauri/src/api/*.rs`，编排逻辑不放在 `lib.rs`。

2. **持久化**：通过 `domain/repository.rs` 的 trait 与 `infrastructure/repositories.rs` 实现；SQL 与表结构以 `kernel/crates/oclive_kernel_host/migrations/001_init.sql` 为准（如 `role_runtime`、`long_term_memory`）；禁止虚构表名。

3. **Tauri**：命令在 `distros/desktop-tauri/src/api/*.rs`，仅在 `distros/desktop-tauri/src/lib.rs` 使用 `tauri::generate_handler!` 注册；保留 `.setup` 与 `AppState::manage`。

4. **DTO**：前后端契约以 `kernel/crates/oclive_kernel_types/src/models/dto/mod.rs` 为准；回复字段为 **`reply`**，不是 `response`；`Emotion` 枚举见 `models/emotion.rs`（无 Joy/Fearful 等未定义变体）。

5. **Prompt**：`PromptBuilder::build_prompt` 定义在 `kernel/crates/oclive_kernel_runtime/src/domain/prompt_builder/mod.rs`（段落公式在同目录 `sections.rs`；经 `oclive_kernel_host::domain` re-export）；**单参数 `input: &PromptInput<'_>`**，返回 `String`（**不是 `Result`，不要用 `?`**）。通用对话纪律收敛在内核常量 `KERNEL_DIALOGUE_GUARDRAILS`（每轮恒追加；包级 `reply_quality_anchor` 仅替换 `DEFAULT_REPLY_QUALITY_ANCHOR`，**不可替换** guardrails）。

6. **工程纪律与关键路径**：见 [`handoff/BUS_FACTOR_NOTES.md`](handoff/BUS_FACTOR_NOTES.md)（编排入口、错误码、迁移）；技术债与冻结项见 [`handoff/TECHNICAL_DEBT_INVENTORY.md`](handoff/TECHNICAL_DEBT_INVENTORY.md)。**勿**将已归档的 `handoff/04_4.6_PROJECT_TRUTH_CHECKLIST.md` 或 `handoff/archive/WEEKLY_DEV_GUIDE.md` 当作当前 truth。

7. **命名 SSOT**：canonical import 与禁止别名见 [`creator-docs/NAMING_CONVENTIONS.md`](creator-docs/NAMING_CONVENTIONS.md) §4.2（DTO → `oclive_kernel_types`；trait → `oclive_kernel_contracts`；编排 → `oclive_kernel_host`）。回复字段为 **`reply`**（非 `response`）；六槽键用 `plugin_backends` / `slot_registry.type`（非 `memory_backend` 等自造别名）。

8. **AI 深读索引**：系统了解项目、按任务翻 SSOT → [`handoff/AI_READING_INDEX.md`](handoff/AI_READING_INDEX.md)；改代码前精简门禁 → [`AGENTS.md`](AGENTS.md)。**GitHub README 仅面向人类**。

9. **AI 改动边界（禁止区摘要）**：见 [`handoff/AI_CHANGE_BOUNDARIES.md`](handoff/AI_CHANGE_BOUNDARIES.md)（G1–G17）。角色任务不改 `slot_registry`；不把 RFC Draft 当未实现而删 wiring；路径改动须 grep 全仓 `roles/` / `src-tauri`；改 `Cargo.lock` 须 `cargo audit` 并更新 `KNOWN_VULNERABILITIES.md`。**关联改动按能力闭环**核对生产者 → 契约 → 适配/权限 → 消费者 → 状态/回退 → 测试（G17），禁止只改单端后声称完成；Chat Pro / 目录插件 / 插槽改动跑 `npm run check:module-compat`。**动文档前先读** [`handoff/README.md`](handoff/README.md) §文档分责；无 RFC **不新建**顶层 `.md`；**链接代替复制**（G14）。

10. **开发流水线（可选）**：用户要求完整流程时，先读通用 `~/.cursor/skills/dev-pipeline/SKILL.md`，再读本仓 [`.cursor/skills/oclive-dev-pipeline/SKILL.md`](.cursor/skills/oclive-dev-pipeline/SKILL.md)（OCLive 定制层）。
