[package]
name = "oclive_schema"
version = "0.1.0"
edition = "2021"
description = "Pure serde schema types for OCLive (decoupled from validation logic)"
license = "AGPL-3.0-or-later"

[lints]
workspace = true

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
