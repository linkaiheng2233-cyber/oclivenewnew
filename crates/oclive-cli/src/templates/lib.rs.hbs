//! `{{ safe_package_name }}` — oclive-cli 生成的可嵌入库。

{{#if kernel_linked}}
pub use oclive_kernel_runtime as runtime;

/// 内核契约 API 版本（见 `oclive_kernel_runtime::RUNTIME_API_VERSION`）。
pub fn runtime_api_version() -> &'static str {
    oclive_kernel_runtime::RUNTIME_API_VERSION
}
{{else}}
/// 返回脚手架包名（占位 API）。
pub fn oclive_stub_id() -> &'static str {
    "{{ safe_package_name }}"
}
{{/if}}
