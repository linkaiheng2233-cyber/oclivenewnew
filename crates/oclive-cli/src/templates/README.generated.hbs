# {{ project_name }}

由 **oclive-cli** 生成的最小可编译内核骨架（占位实现）。

## 说明

- **当前 `Cargo.toml` 仅依赖 `serde` / `serde_json`**，便于在无完整 `oclive_kernel_runtime` 树的环境下验证目录与配置形状。
- 将 `plugin_backends` 与 `_oclive_cli` 块视为**脚手架元数据**；接入真实宿主时，请替换为与 [PLUGIN_V1.md](https://github.com/oclive-app/oclivenewnew/blob/main/creator-docs/plugin-and-architecture/PLUGIN_V1.md) 一致的枚举值。
- 根目录 **`CONFIG_REFERENCE.md`**：本 `init` 所用预设与七槽对照表；字段级权威说明见主仓 **`creator-docs/cli/SETTINGS_REFERENCE.md`**（与仓库一并检出时）。

## 可选路线

{{#if plugin_kernel_server}}
- **kernel_server**：在 oclivenewnew 工作区构建 `oclive_kernel_server` 后，于此 `Cargo.toml` 添加 path 依赖并替换 `main.rs` 入口。
{{/if}}
{{#if plugin_oocp}}
- **OOCP**：参见主仓 `examples/oocp-test-suite` 与 `creator-docs/oocp/` 索引，将协议测试与无头服务联调。
{{/if}}
{{#if plugin_directory}}
- **目录插件**：预留 `plugins/` 扫描与 manifest 流程，需接入宿主 `DirectoryPluginRuntime` 后再启用。
{{/if}}

## 构建

```bash
cargo build
cargo run
```
