[package]
name = "oclive_core"
version = "0.1.0"
edition = "2021"
description = "Oclive kernel: platform-independent domain logic and OOCP protocol (AGPLv3)"
license = "AGPL-3.0"

[lib]
crate-type = ["lib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"