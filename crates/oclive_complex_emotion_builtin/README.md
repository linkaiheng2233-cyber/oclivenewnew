# oclive_complex_emotion_builtin

**官方默认复杂情感模块**（工程名：设施 crate）。七维→效价/掌控感辅助（feature **`classic`**，默认开；关时恒 `(0,0)`）与可选的关键词 `ComplexEmotionProvider`（`providers`，隐含 `classic`）。门控见 `creator-docs/kernel/FACILITY_CLASSIC_ALGORITHMS_AUDIT.md`。

Directory 侧车示例：`examples/oclive-complex-emotion-builtin-directory/`（`complex_emotion.resolve_turn`）。
